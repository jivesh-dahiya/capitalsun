---
version: 1
slug: "landing"
primary_target: "landing"
related_targets: []
---

## Direction contract

THESIS: The jobsite itself is the hero — a real Australian rooftop install photographed in warm, physical light — replacing the prior UI-proof-as-hero (dark band + pipeline strip) with a photography-led "real work getting done" opening, refusing the generic dark-SaaS-dashboard hero the previous build used.

OWN-WORLD: Keeps Capitalsun's actual tokens (burnt-orange `--primary`, IBM Plex Sans/Mono, `6/8/12px` radii) but raises photography to page scale — a full-bleed AI-generated rooftop-install hero photo, a staggered jobsite photo gallery, and photo cards for the three real capabilities — translating the reference's travel-photography, search-bar-over-hero, tag-badge, staggered-gallery, metadata-card structure into solar-install imagery and real product facts (no invented customer counts or review quotes — those chips carry only true, verifiable statements).

STORY: Visitor lands on a real rooftop mid-install (not a screenshot), the STC calculator sits where a destination search bar would (styled as the hero's overlaid utility bar: System size / State / Install year / Calculate), scrolls into a "why Capitalsun" gallery of real jobsite moments, then a photo-card grid of the three real mechanisms (satellite roof design / compliance paperwork / lead intake), then the existing team CTA and footer unchanged.

FIRST VIEWPORT: full-bleed rooftop-solar hero photo (golden hour), nav overlaid transparent top, headline + subhead top-left in white, the STC calculator restyled as a light utility bar anchored across the hero's bottom edge (System size / State / Install year fields + a "Calculate" action, live-computed result appearing inline), two factual tag badges below the nav-left (no fabricated stats).

FORM: reference-adapted "photography-led marketplace" structure; direction pinned directly by the user's supplied reference screenshot and explicit "entire landing page style" confirmation — no concept-seed roll, per the brief-pinned-direction rule.

FINISH: unreviewed and undocumented is unfinished; this build ends with a self-run screenshot review (desktop + mobile), the detector pass, and DESIGN.md updated from the shipped result.
